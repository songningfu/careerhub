name = "careerhub-api"
main = "worker.js"
compatibility_date = "2024-10-01"

# 环境变量在 Cloudflare Dashboard 配置（Secret）
# - LLM_PROVIDER: openai | anthropic | deepseek（默认 openai）
# - OPENAI_API_KEY / ANTHROPIC_API_KEY / DEEPSEEK_API_KEY

# 可选：开启 KV 限流（更稳定）
# [[kv_namespaces]]
# binding = "RATE_KV"
# id = "你创建的 KV 的 id"
